#! /usr/bin/bash 

# use "> filename" to echo content to the file, if the file is not there then it will create the file and if the file is already there then it will override the content of the file 

echo "This content will be printed to file1.txt in the same folder" > ./file1.txt
